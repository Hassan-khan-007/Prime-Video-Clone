apiVersion: v1
kind: Service
metadata:
  name: prime-video-service
spec:
  selector:
    app: prime-video
  ports:
    - protocol: TCP
      port: 3000        
      targetPort: 3000  
  type: ClusterIP
#apiVersion: v1
#kind: Service
#metadata:
 # name: prime-video-service
#spec:
 # selector:
  #  app: prime-video
  #ports:
   # - protocol: TCP
   #   port: 80
    #  targetPort: 3000
  #type: NodePort
